:ruby
	item = locals["data"]

	lat = item["lat"].to_f.round 2
	lng = item["lon"].to_f.round 2
	altitude = item["alt"].to_f.round 0

%h1.entry-title
	was at #{lat}° N, #{lng}° W
	= surround "(", ")" do
		#{altitude}
		%abbr{ title: "meters" }<
			m
		above sea level
