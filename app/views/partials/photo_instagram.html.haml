- item = locals["data"]

%h1.entry-title
	posted a photo

- if locals["extras"]["full_view"]
	%section.entry-content
		%a{ href: locals["url"] }
			%img{ src: eval(item["images"])["standard_resolution"]["url"] }

		- if item["caption"]
			%blockquote
				%p
					= nl2br eval(item["caption"])["text"]
