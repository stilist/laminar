:ruby
	item = locals["data"]

%h1.entry-title
	favorited
	%a{ href: locals["url"] }
		#{item[:title]}

- if locals["extras"]["full_view"] && item[:description] != ""
	%section.entry-content
		%blockquote
			%p
				= item[:description]
